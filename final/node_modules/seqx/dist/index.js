var ParX, SeqX, parx, seqx;

SeqX = require("./lib/SeqX");

ParX = require("./lib/ParX");

seqx = function(opts) {
  return new SeqX(opts);
};

parx = function(opts) {
  return new ParX(opts);
};

seqx.SeqX = SeqX;

seqx.par = parx;

parx.ParX = ParX;

module.exports = seqx;
